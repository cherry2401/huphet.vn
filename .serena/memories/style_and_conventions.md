Codebase uses TypeScript with Next.js App Router conventions. Files use double quotes and semicolons consistently. Route handlers and utilities appear modularized under src/app and src/lib. Security-related config is split between middleware.ts, next.config.ts, and lib helpers. Prefer targeted edits and preserve existing naming and route structure. No broad formatting tool is defined in package scripts; linting is via ESLint and tests via Vitest.